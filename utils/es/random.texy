Generador de cadenas aleatorias
*******************************

.[perex]
[api:Nette\Utils\Random] es una clase estática para generar cadenas pseudoaleatorias criptográficamente seguras.


Instalación:

```shell
composer require nette/utils
```


generate(int $length=10, string $charlist='0-9a-z'): string .[method]
---------------------------------------------------------------------

Genera una cadena aleatoria de longitud dada a partir de los caracteres especificados en el segundo argumento. Admite intervalos, como `0-9` o `A-Z`.

```php
use Nette\Utils\Random;

Random::generate(10);       // '6zq3a1nl8n'
Random::generate(5, 'A-Z'); // 'HLKUR'
```
