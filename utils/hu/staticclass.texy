Statikus osztályok
******************

.[perex]
A StaticClass statikus osztályok megjelölésére szolgál.


Telepítés:

```shell
composer require nette/utils
```

A statikus osztályokat, azaz azokat az osztályokat, amelyek nem példányosításra szolgálnak, megjelölheti a [api:Nette\StaticClass] trait-tel:

```php
class Strings
{
	use Nette\StaticClass;
}
```
