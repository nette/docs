Latte で SQL クエリを書くには？
*********************

.[perex]
Latte は、本当に複雑な SQL クエリをより読みやすい形で生成するのにも役立ちます。

SQL クエリの組み立てに多くの条件や変数が絡む場合、それを Latte で書くと見通しが大きく改善します。ごく簡単な例を挙げましょう。

```latte
SELECT users.* FROM users
	LEFT JOIN users_groups ON users.user_id = users_groups.user_id
 	LEFT JOIN groups ON groups.group_id = users_groups.group_id
	{ifset $country} LEFT JOIN country ON country.country_id = users.country_id {/ifset}
WHERE groups.name = 'Admins' {ifset $country} AND country.name = {$country} {/ifset}
```

`$latte->setContentType()` を使って、内容を HTML ではなくプレーンテキストとして扱うよう Latte に伝えます。そのうえで、データベースドライバを使って文字列を正しくエスケープする関数を用意します。

```php
$db = new PDO(/* ... */);

$latte = new Latte\Engine;
$latte->setContentType(Latte\ContentType::Text);
$latte->addFilter('escape', fn($val) => match (true) {
	is_string($val) => $db->quote($val),
	is_int($val), is_float($val) => (string) $val,
	is_bool($val) => $val ? '1' : '0',
	is_null($val) => 'NULL',
	default => throw new Exception('Unsupported type'),
});
```

使い方はこのようになります。

```php
$sql = $latte->renderToString('query.sql.latte', ['country' => $country]);
$result = $db->query($sql);
```

*この例には Latte v3.0.5 以上が必要です。*
