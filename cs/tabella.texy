Tabella
*******

.[perex]
Komponenta pro vizualizaci databázových tabulek nad databázovým layerem Dibi.

|Download | http://tabella.goout.cz/tabella.zip]
|Demo | http://tabella.goout.cz/
|Autor | Vojtech Knyttl
|Licence | New BSD License

Priklad zakladniho pouziti
=========================

Pro vsechny nasledujici priklady predpokladejme nasledujici DB model:

/---php
	public static function getDataSource() {
	}
\---

Komponentu vytvorime tovarnickou na komponenty nasledvone.
/---php
	protected function createComponentBasicTabella( $name ) {
            $grid = new Tabella( UsersModel::getDataSource() );
            $grid->addColumn( "Id", "id", array( "width" => 30 ) );
            $grid->addColumn( "Surame", "surname", array( "width" => 100 ) );
            $grid->addColumn( "Name", "name", array( "width" => 100 ) );
            $grid->addColumn( 'Sex', 'sex', array( "width" => 50 ) );
            $grid->addColumn( "Sons", "sons", array( "width" => 65 ) );
            $grid->addColumn( "Daughters", "daughters", array( "width" => 65 ) );
            $grid->addColumn( 'Born', 'born', array( "type" => Tabella::DATETIME ) );
            $grid->addColumn( 'Dead', 'dead', array( "type" => Tabella::DATETIME ) );
            $this->addComponent( $grid, $name );
        }
\---

Stredem celeho pouziti je prikaz addColumn pro pridani sloupce. Prvni parametr udava jmeno, ktere bude uvedeno v hlavicce, druha polozka je nazev sloupce v DB a treti je pole parametru. V zakladnim pouziti si zatim vystacime s sirkou (udanou v pixelech) a typem. Typ muze nabyvat hodnot TEXT, TEXTAREA, SELECT, CHECKBOX, DATETIME, DATE, TIME. Polozky TEXTAREA, SELECT, CHECKBOX se tykaji pouze inline editace (viz dale).

{{author: knyttl|2867}}
