Migracja z PHP do Latte
***********************

.[perex]
Przenosisz stary projekt napisany w czystym PHP na Latte? Mamy narzędzie, które ułatwi migrację. [Wypróbuj je online |https://fiddle.nette.org/php2latte/].

Narzędzie możesz pobrać z [GitHuba|https://github.com/nette/latte-tools] albo zainstalować przez Composera:

```shell
composer create-project latte/tools
```

Konwerter nie opiera się na prostych podmianach wyrażeniami regularnymi, lecz korzysta z prawdziwego parsera PHP, dzięki czemu radzi sobie z dowolnie złożoną składnią.

Do konwersji z PHP na Latte służy skrypt `php-to-latte.php`:

```shell
php php-to-latte.php input.php [output.latte]
```


Przykład
--------

Plik wejściowy może wyglądać tak (to fragment kodu forum PunBB):

```php
<h1><span><?= $lang_common['User list'] ?></span></h1>

<div class="blockform">
	<form id="userlist" method="get" action="userlist.php">
		<div class="infldset">
<?php
foreach ($result as $cur_group) {
	if ($cur_group['g_id'] == $show_group) {
		echo "\n\t\t" . '<option value="' . $cur_group['g_id'] . '" selected="selected">'
			. htmlspecialchars($cur_group['g_title']) . '</option>';
	} else {
		echo "\n\t\t" . '<option value="' . $cur_group['g_id'] . '">'
			. htmlspecialchars($cur_group['g_title']) . '</option>';
	}
}
?>
			</select>
			<p class="clearb"><?= $lang_ul['User search info'] ?></p>
		</div>
	</form>
</div>
```

Wygeneruje taki szablon:

```latte
<h1><span>{$lang_common['User list']}</span></h1>

<div class="blockform">
	<form id="userlist" method="get" action="userlist.php">
		<div class="infldset">
{foreach $result as $cur_group}
	{if $cur_group[g_id] == $show_group}
		<option value="{$cur_group[g_id]}" selected="selected">{$cur_group[g_title]}</option>
	 {else}
		<option value="{$cur_group[g_id]}">{$cur_group[g_title]}</option>
	{/if}
{/foreach}			</select>
			<p class="clearb">{$lang_ul['User search info']}</p>
		</div>
	</form>
</div>
```
